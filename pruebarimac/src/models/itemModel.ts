// src/models/itemModel.ts
export interface Item {
    id: string;
    nombre: string;  // Traducción de "name"
    descripcion: string;  // Traducción de "description"
    fechaCreacion: string; // Fecha de creación
  }
   
